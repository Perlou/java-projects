{
  "name": "mall-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:web": "pnpm -F @mall/web dev",
    "dev:admin": "pnpm -F @mall/admin dev",
    "build:web": "pnpm -F @mall/web build",
    "build:admin": "pnpm -F @mall/admin build",
    "build:all": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "~5.6.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
